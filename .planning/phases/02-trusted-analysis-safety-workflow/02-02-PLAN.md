---
phase: 02-trusted-analysis-safety-workflow
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/modules/findings/lib/useFindings.ts
  - src/modules/findings/FindingsDashboard.tsx
  - src/modules/findings/FindingDetailSheet.tsx
  - src/modules/findings/FindingsDashboard.test.ts
  - src/app/App.tsx
autonomous: true
requirements:
  - PLAN-02
  - PLAN-03
  - APLY-01
must_haves:
  truths:
    - Opening a finding must show finding detail, affected files, and diff preview per D-03 and D-04.
    - Review remains lightweight and specialized; no full plan workspace or apply affordances appear in this phase.
    - Diff review must be available before any future apply path per APLY-01.
  artifacts:
    - path: src/modules/findings/FindingDetailSheet.tsx
      provides: Trusted review panel with rationale, affected files, and diff preview
    - path: src/modules/findings/FindingsDashboard.tsx
      provides: Findings-first review flow with lightweight review grouping preparation
  key_links:
    - from: src/modules/findings/lib/useFindings.ts
      to: src/modules/findings/FindingDetailSheet.tsx
      via: selected finding review state
      pattern: "selectedFinding"
---

## Phase Goal

**As a** Java developer, **I want to** inspect a finding with affected files and diff preview inside the findings workflow, **so that** review feels trustworthy before any apply feature exists.

<objective>
Turn the existing finding detail path into a true review surface for PLAN-02, PLAN-03, and APLY-01.

Purpose: keep review inside the specialized dashboard while making proposed changes inspectable.
Output: detail review panel with diff preview, lightweight grouping preparation, and tests proving the review-only contract.
</objective>

<execution_context>
@C:/Users/serasa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/serasa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-trusted-analysis-safety-workflow/02-CONTEXT.md
@.planning/phases/02-trusted-analysis-safety-workflow/02-RESEARCH.md
@.planning/phases/02-trusted-analysis-safety-workflow/02-PATTERNS.md
@.planning/phases/02-trusted-analysis-safety-workflow/02-VALIDATION.md
@.planning/phases/02-trusted-analysis-safety-workflow/02-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing review-surface tests for diff preview and review-only detail behavior</name>
  <files>src/modules/findings/FindingsDashboard.test.ts</files>
  <read_first>src/modules/findings/FindingsDashboard.test.ts; src/modules/preview/PreviewPane.test.ts; .planning/phases/02-trusted-analysis-safety-workflow/02-VALIDATION.md</read_first>
  <acceptance_criteria>
    - Tests assert finding detail includes affected files, rationale, and diff preview.
    - Tests assert any review grouping metadata stays lightweight and does not expose apply actions.
    - `corepack pnpm test -- src/modules/findings/FindingsDashboard.test.ts` fails before implementation and passes after.
  </acceptance_criteria>
  <action>Extend `src/modules/findings/FindingsDashboard.test.ts` with failing coverage for Phase 2 review behavior. Lock the diff-preview path, the review-only boundary, and the continued findings-first hierarchy before implementation.</action>
  <verify>
    <automated>corepack pnpm test -- src/modules/findings/FindingsDashboard.test.ts</automated>
  </verify>
  <done>Frontend review tests fail first, then pass after the review surface is expanded.</done>
</task>

<task type="auto">
  <name>Task 2: Implement finding detail review with diff preview and lightweight grouping preparation</name>
  <files>src/modules/findings/lib/useFindings.ts, src/modules/findings/FindingsDashboard.tsx, src/modules/findings/FindingDetailSheet.tsx, src/app/App.tsx</files>
  <read_first>src/modules/findings/lib/useFindings.ts; src/modules/findings/FindingsDashboard.tsx; src/modules/findings/FindingDetailSheet.tsx; src/modules/source-control/SourceControlPanel.tsx; src/modules/source-control/useSourceControlPanel.ts; .planning/phases/02-trusted-analysis-safety-workflow/02-PATTERNS.md</read_first>
  <acceptance_criteria>
    - Selected findings show rationale, affected files, and diff preview inside the dedicated review path.
    - Any review grouping preparation stays lightweight and review-only, without a full plan workspace.
    - `corepack pnpm test -- src/modules/findings/FindingsDashboard.test.ts` passes.
  </acceptance_criteria>
  <action>Expand the findings module so the selected finding becomes a trustworthy review object. Add diff preview data handling, enrich the detail sheet, and keep the review path inside the specialized dashboard. If lightweight grouping metadata is introduced, keep it preparatory only and do not expose apply actions or a full refactor-plan workspace.</action>
  <verify>
    <automated>corepack pnpm test -- src/modules/findings/FindingsDashboard.test.ts</automated>
  </verify>
  <done>Reviewing a finding becomes meaningfully deeper while the product still reads as lightweight and review-first.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native findings→frontend review surface | Native findings and diff data cross into user-facing review UI |
| review UI→future apply expectations | Users may infer apply support from the review surface even though Phase 2 is review-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Spoofing | review UI | mitigate | Clearly render review-only state and avoid apply affordances. |
| T-02-05 | Information Disclosure | diff preview | mitigate | Keep preview scoped to authorized repo content and normalized native DTOs. |
</threat_model>

<verification>
`corepack pnpm test -- src/modules/findings/FindingsDashboard.test.ts`
</verification>

<success_criteria>
Opening a finding now feels like real review, not just reading a heuristic summary, and the workflow still stays review-only.
</success_criteria>
