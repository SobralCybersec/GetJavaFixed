---
phase: 02-trusted-analysis-safety-workflow
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src-tauri/src/modules/git/commands.rs
  - src-tauri/src/modules/git/operations.rs
  - src-tauri/src/modules/fs/file.rs
  - src-tauri/src/modules/mod.rs
  - src-tauri/src/lib.rs
  - src-tauri/tests/java_safety_flow.rs
  - src/modules/findings/lib/useFindings.ts
  - src/modules/findings/FindingsDashboard.tsx
autonomous: true
requirements:
  - SAFE-01
  - SAFE-02
  - SAFE-03
must_haves:
  truths:
    - Safety must be git-first when the repo is under version control per D-05.
    - Non-git repos must fall back to backup-copy protection per D-06.
    - Phase 2 must remain review-only even while safety and rollback readiness are surfaced per D-08 and D-09.
  artifacts:
    - path: src-tauri/tests/java_safety_flow.rs
      provides: Native safety regression coverage for git-first and backup-fallback contracts
    - path: src/modules/findings/FindingsDashboard.tsx
      provides: User-visible safety state inside the review path
  key_links:
    - from: src-tauri/src/modules/git/commands.rs
      to: src/modules/findings/lib/useFindings.ts
      via: invoke command
      pattern: "git_panel_snapshot"
---

## Phase Goal

**As a** Java developer, **I want to** understand what rollback and safety path exists for the repo I am reviewing, **so that** I can trust the workflow before any later apply phase arrives.

<objective>
Establish the Phase 2 safety-state and rollback-readiness contract for SAFE-01 through SAFE-03.

Purpose: make trust explicit before apply exists.
Output: native git-first versus backup-fallback safety contract, frontend safety-state display, and regression tests.
</objective>

<execution_context>
@C:/Users/serasa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/serasa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-trusted-analysis-safety-workflow/02-CONTEXT.md
@.planning/phases/02-trusted-analysis-safety-workflow/02-RESEARCH.md
@.planning/phases/02-trusted-analysis-safety-workflow/02-PATTERNS.md
@.planning/phases/02-trusted-analysis-safety-workflow/02-VALIDATION.md
@.planning/phases/02-trusted-analysis-safety-workflow/02-01-PLAN.md
@.planning/phases/02-trusted-analysis-safety-workflow/02-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing safety-flow coverage for git-first and backup-fallback contracts</name>
  <files>src-tauri/tests/java_safety_flow.rs</files>
  <read_first>src-tauri/tests/git_operations.rs; src-tauri/tests/common/mod.rs; src-tauri/src/modules/git/operations.rs; src-tauri/src/modules/fs/file.rs; .planning/phases/02-trusted-analysis-safety-workflow/02-VALIDATION.md</read_first>
  <acceptance_criteria>
    - Tests cover git-backed repos reporting git-first safety.
    - Tests cover non-git repos reporting backup-copy fallback readiness.
    - `cargo test --locked --test java_safety_flow` fails before implementation and passes after.
  </acceptance_criteria>
  <action>Create `src-tauri/tests/java_safety_flow.rs` to lock the safety contract before implementation. Exercise both git-backed and non-git Java repo fixtures and assert rollback-readiness state stays review-only.</action>
  <verify>
    <automated>cargo test --locked --test java_safety_flow</automated>
  </verify>
  <done>Safety tests fail first, then pass once native safety-state behavior exists.</done>
</task>

<task type="auto">
  <name>Task 2: Implement native safety snapshot and surface it in the review path</name>
  <files>src-tauri/src/modules/git/commands.rs, src-tauri/src/modules/git/operations.rs, src-tauri/src/modules/fs/file.rs, src-tauri/src/modules/mod.rs, src-tauri/src/lib.rs, src/modules/findings/lib/useFindings.ts, src/modules/findings/FindingsDashboard.tsx</files>
  <read_first>src-tauri/src/modules/git/commands.rs; src-tauri/src/modules/git/operations.rs; src-tauri/src/modules/fs/file.rs; src/modules/findings/lib/useFindings.ts; src/modules/findings/FindingsDashboard.tsx; .planning/phases/02-trusted-analysis-safety-workflow/02-RESEARCH.md; .planning/phases/02-trusted-analysis-safety-workflow/02-PATTERNS.md</read_first>
  <acceptance_criteria>
    - Native code exposes a typed safety snapshot that distinguishes git-first and backup-fallback states.
    - Frontend review surface clearly shows the current safety path and rollback readiness.
    - `cargo test --locked --test java_safety_flow` passes.
  </acceptance_criteria>
  <action>Add the native safety-state contract needed for Phase 2 review trust. Reuse existing git repo/status primitives where possible, define backup-fallback readiness for non-git repos inside the Rust filesystem boundary, and surface the result inside the findings review UI. Keep the contract explicitly review-only: no apply behavior, only trust and rollback-readiness signaling.</action>
  <verify>
    <automated>cargo test --locked --test java_safety_flow</automated>
  </verify>
  <done>Users can tell whether the repo is protected by git-first rollback or backup fallback before any future apply path appears.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| review UI→safety contract | Users rely on displayed safety information before trusting future write paths |
| safety snapshot→native repo/file primitives | Native code must truthfully reflect repo and fallback state without frontend guesses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Spoofing | safety state | mitigate | Compute safety state only from native git/filesystem primitives. |
| T-02-07 | Tampering | backup fallback metadata | mitigate | Keep fallback preparation inside authorized Rust file boundary. |
| T-02-08 | Information Disclosure | rollback readiness details | accept | Expose only safety path and readiness state, not unnecessary repo internals. |
</threat_model>

<verification>
`cargo test --locked --test java_safety_flow`
</verification>

<success_criteria>
Phase 2 makes trust explicit: users can see how rollback would work before any future write path exists.
</success_criteria>
