---
phase: 02-trusted-analysis-safety-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/modules/analysis/mod.rs
  - src-tauri/src/modules/mod.rs
  - src-tauri/src/lib.rs
  - src-tauri/tests/java_analysis_review.rs
autonomous: true
requirements:
  - ANLY-01
  - ANLY-02
  - ANLY-03
must_haves:
  truths:
    - Phase 2 analysis must stay stronger-local-heuristics-first per D-01 and D-02.
    - Findings must cover safe, performance, and modernization categories with deterministic ranking.
    - Analysis remains review-only and must not widen into apply behavior in this plan.
  artifacts:
    - path: src-tauri/src/modules/analysis/mod.rs
      provides: Trusted repo-wide Java scan with richer findings DTO and deterministic scoring
    - path: src-tauri/tests/java_analysis_review.rs
      provides: Native regression coverage for safe, performance, and modernization findings
  key_links:
    - from: src-tauri/src/lib.rs
      to: src-tauri/src/modules/analysis/mod.rs
      via: tauri::generate_handler registration
      pattern: "phase1_analysis_start"
---

## Phase Goal

**As a** Java developer, **I want to** run a more credible repo-wide analysis for safe, performance, and modernization findings, **so that** the findings queue feels trustworthy enough to review before any write path exists.

<objective>
Deepen the Phase 1 analysis engine into a trustworthy deterministic repo-wide scan for ANLY-01 through ANLY-03.

Purpose: improve credibility without committing to a structured transformation engine yet.
Output: richer native findings categories, deterministic scoring, and tests proving safe/performance/modernization coverage.
</objective>

<execution_context>
@C:/Users/serasa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/serasa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-trusted-analysis-safety-workflow/02-CONTEXT.md
@.planning/phases/02-trusted-analysis-safety-workflow/02-RESEARCH.md
@.planning/phases/02-trusted-analysis-safety-workflow/02-PATTERNS.md
@.planning/phases/02-trusted-analysis-safety-workflow/02-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing native trusted-analysis coverage for safe, performance, and modernization findings</name>
  <files>src-tauri/tests/java_analysis_review.rs</files>
  <read_first>src-tauri/tests/java_repo_intake.rs; src-tauri/tests/git_operations.rs; src-tauri/tests/common/mod.rs; .planning/phases/02-trusted-analysis-safety-workflow/02-VALIDATION.md</read_first>
  <acceptance_criteria>
    - Coverage asserts at least one deterministic safe, performance, and modernization finding from real fixture repo content.
    - Tests prove ranking is deterministic and review-only analysis does not depend on UI state.
    - `cargo test --locked --test java_analysis_review` fails before implementation and passes after.
  </acceptance_criteria>
  <action>Create `src-tauri/tests/java_analysis_review.rs` with fixture repos that exercise safety-style, performance-style, and modernization-style Java findings. Assert deterministic ordering, category coverage, and clear rationale fields using the module boundary rather than frontend assumptions.</action>
  <verify>
    <automated>cargo test --locked --test java_analysis_review</automated>
  </verify>
  <done>Native analysis tests fail first, then pass once the richer findings contract exists.</done>
</task>

<task type="auto">
  <name>Task 2: Expand the native analysis module into a richer deterministic repo-wide scan</name>
  <files>src-tauri/src/modules/analysis/mod.rs, src-tauri/src/modules/mod.rs, src-tauri/src/lib.rs</files>
  <read_first>src-tauri/src/modules/analysis/mod.rs; src-tauri/src/modules/fs/watch.rs; src-tauri/src/modules/workspace.rs; .planning/phases/02-trusted-analysis-safety-workflow/02-RESEARCH.md; .planning/phases/02-trusted-analysis-safety-workflow/02-PATTERNS.md</read_first>
  <acceptance_criteria>
    - Native findings payload covers safe, performance, and modernization classes with deterministic scoring.
    - Analysis continues to run within the existing authorized Rust boundary and remains review-only.
    - `cargo test --locked --test java_analysis_review` passes.
  </acceptance_criteria>
  <action>Evolve `src-tauri/src/modules/analysis/mod.rs` from a starter heuristic scanner into a richer deterministic repo-wide analysis path. Add additional Java rule coverage and normalized findings metadata while preserving the current progress/status contract shape wherever practical. Keep the work stronger-local-heuristics-first per D-01 and D-02, and do not introduce transformation or apply behavior.</action>
  <verify>
    <automated>cargo test --locked --test java_analysis_review</automated>
  </verify>
  <done>The backend can produce a more credible multi-category findings set without widening into a structured engine or write path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→analysis command | Untrusted repo paths and analysis triggers cross into native scanning logic |
| analysis command→filesystem | Native code inspects authorized Java repo contents to build findings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | trusted analysis scan | mitigate | Reuse workspace authorization and canonicalized root access before repo-wide scanning. |
| T-02-02 | Spoofing | findings credibility | mitigate | Keep scoring deterministic and grounded in local scan rules rather than opaque ranking. |
| T-02-03 | Denial of Service | repo-wide scan | mitigate | Keep file traversal bounded and skip generated directories or oversized repo branches as needed. |
</threat_model>

<verification>
`cargo test --locked --test java_analysis_review`
</verification>

<success_criteria>
Phase 2 analysis feels materially more credible than Phase 1 while remaining deterministic, local, and review-only.
</success_criteria>
