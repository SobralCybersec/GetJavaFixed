---
phase: 01-java-repo-intake-findings-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/modules/java_repo.rs
  - src-tauri/src/modules/mod.rs
  - src-tauri/src/lib.rs
  - src-tauri/tests/java_repo_intake.rs
autonomous: true
requirements:
  - REPO-01
  - REPO-02
must_haves:
  truths:
    - User can only advance with a selected-root Maven or Gradle repository per D-13 and D-14.
    - User-facing repo support state comes from one native readiness contract rather than frontend guesses per D-04 and D-22.
  artifacts:
    - path: src-tauri/src/modules/java_repo.rs
      provides: Strict selected-root readiness detection and typed repo metadata
    - path: src-tauri/tests/java_repo_intake.rs
      provides: Native regression coverage for Maven, Gradle, and unsupported roots
  key_links:
    - from: src-tauri/src/lib.rs
      to: src-tauri/src/modules/java_repo.rs
      via: tauri::generate_handler registration
      pattern: "java_repo_readiness"
---

## Phase Goal

**As a** Java developer, **I want to** open a Maven or Gradle repository and move into a specialized intake flow, **so that** I can start refactor analysis from a Java-focused shell.

<objective>
Create the native repo-readiness skeleton for Phase 1 per D-04, D-13, D-14, D-22, and D-23.

Purpose: lock support detection behind Rust authorization before any UI slice trusts a selected folder.
Output: a tested Tauri command that classifies the selected root as Maven, Gradle, or unsupported and returns minimal repo metadata.
</objective>

<execution_context>
@C:/Users/serasa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/serasa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-java-repo-intake-findings-shell/01-CONTEXT.md
@.planning/phases/01-java-repo-intake-findings-shell/01-RESEARCH.md
@.planning/phases/01-java-repo-intake-findings-shell/01-PATTERNS.md
@.planning/phases/01-java-repo-intake-findings-shell/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing native intake coverage for strict selected-root detection</name>
  <files>src-tauri/tests/java_repo_intake.rs</files>
  <read_first>src-tauri/tests/fs_search.rs; src-tauri/tests/common/mod.rs; src-tauri/src/modules/workspace.rs; .planning/phases/01-java-repo-intake-findings-shell/01-VALIDATION.md</read_first>
  <acceptance_criteria>
    - `src-tauri/tests/java_repo_intake.rs` contains coverage for root-level `pom.xml`, `build.gradle`, `build.gradle.kts`, and unsupported Java-like folders.
    - At least one test asserts the Phase 1 rejection message path required by D-15 and D-17.
    - `<verify><automated>` runs red before implementation and green after implementation.
  </acceptance_criteria>
  <action>Create `src-tauri/tests/java_repo_intake.rs` as the Phase 1 happy-path gate for REPO-01 and REPO-02. Use the `FsFixture` pattern to build selected-root fixtures only, assert Maven/Gradle detection from `pom.xml`, `build.gradle`, and `build.gradle.kts` per D-14, and assert unsupported roots stay blocked with explicit unsupported reasoning per D-13, D-15, and D-17. Keep tests at the module boundary so the eventual `java_repo_readiness` contract can be exercised without frontend assumptions.</action>
  <verify>
    <automated>cargo test --locked --test java_repo_intake</automated>
  </verify>
  <done>Native tests fail first for missing readiness behavior, then pass once the readiness command and helpers exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the authorized readiness command and wire it into Tauri</name>
  <files>src-tauri/src/modules/java_repo.rs, src-tauri/src/modules/mod.rs, src-tauri/src/lib.rs</files>
  <read_first>src-tauri/src/modules/workspace.rs; src-tauri/src/modules/fs/watch.rs; src-tauri/src/lib.rs; src-tauri/src/modules/mod.rs; .planning/phases/01-java-repo-intake-findings-shell/01-RESEARCH.md; .planning/phases/01-java-repo-intake-findings-shell/01-PATTERNS.md</read_first>
  <acceptance_criteria>
    - `java_repo_readiness` is registered in `src-tauri/src/lib.rs` and exported from `src-tauri/src/modules/mod.rs`.
    - The readiness DTO exposes only the minimum state required by D-22: support status, detected build tool, repo name, and reason.
    - The implementation authorizes and canonicalizes the selected root before reading manifests, and it does not surface git state per D-23.
    - `cargo test --locked --test java_repo_intake` passes.
  </acceptance_criteria>
  <action>Add `src-tauri/src/modules/java_repo.rs` with a `#[tauri::command]` named `java_repo_readiness` plus small pure helpers for repo-name extraction and selected-root build-file detection. Reuse `WorkspaceEnv` and `WorkspaceRegistry` authorization patterns from `workspace.rs`, inspect only the chosen root, and return a minimal typed contract that enforces D-04, D-13, D-14, D-22, and D-23. Wire the module through `src-tauri/src/modules/mod.rs` and register the command in `src-tauri/src/lib.rs` without introducing generic-shell fallback behavior.</action>
  <verify>
    <automated>cargo test --locked --test java_repo_intake</automated>
  </verify>
  <done>The backend can classify an authorized selected root as Maven, Gradle, or unsupported, and the tests prove unsupported roots remain blocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→readiness command | Untrusted folder paths cross from React into native filesystem logic |
| readiness command→filesystem | Native code reads manifest files from the authorized workspace root |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `java_repo_readiness` | mitigate | Canonicalize and authorize every selected path via `WorkspaceRegistry` before manifest reads. |
| T-01-02 | Spoofing | readiness DTO | mitigate | Derive support state only from native selected-root detection, not frontend heuristics, per D-14 and D-22. |
| T-01-03 | Information Disclosure | readiness error messages | accept | Return only repo name, build tool, and unsupported reason; no file contents or git metadata per D-23. |
| T-01-SC | Tampering | cargo/pnpm dependency surface | mitigate | No new package install in this plan; keep dependency surface unchanged while adding native coverage. |
</threat_model>

<verification>
`cargo test --locked --test java_repo_intake`
</verification>

<success_criteria>
The repo-readiness contract exists, is covered by native tests, and enforces strict selected-root Maven/Gradle support before any Phase 1 UI flow can advance.
</success_criteria>

<output>
Create `.planning/phases/01-java-repo-intake-findings-shell/01-01-SUMMARY.md` when done
</output>
