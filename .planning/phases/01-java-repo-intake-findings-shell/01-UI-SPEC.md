---
phase: 1
slug: java-repo-intake-findings-shell
status: draft
shadcn_initialized: true
preset: radix-luma / mist / hugeicons / inter
created: 2026-06-01
---

# Phase 1 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn |
| Preset | `radix-luma` preset `b1XWJ8t6` with `mist` base color |
| Component library | radix |
| Icon library | hugeicons |
| Font | Inter Variable |

---

## Phase Surface Contract

- Entry state must replace the generic workspace shell with a dedicated Java repo home.
- The repo home must present one primary action only: `Start full analysis`.
- Readiness must stay minimal: support status, detected build tool, and next action only.
- Unsupported repositories must hard block the flow in a centered rejection state with one recovery action: `Choose another folder`.
- The dashboard must be findings-first, with findings occupying the primary column and a right-side detail panel for deeper context.
- The detail panel must include affected files as the most important secondary surface.
- The loaded repo header must remain as a persistent 40px top bar showing repo name and build tool only.
- The list must stay as one ranked queue ordered by priority, not grouped by category.
- Each finding row must keep a small category badge, but priority treatment must be visually stronger than category treatment.

---

## Interaction Contract

- Selecting a valid repo must move the user directly from readiness to the findings dashboard.
- Starting analysis must keep the user on the dashboard and replace the empty list with live progress plus skeleton findings rows.
- Analysis progress must appear inline at the top of the findings column, not in a modal or separate screen.
- Phase 1 analysis is watch-only. No apply, edit, rollback, or git-state controls may appear in this phase.
- When analysis completes, the highest-priority finding must auto-select and open in the right-side detail panel.
- Finding rows must be compact and scannable: target 72px to 88px height with one-line title, one-line rationale, and compact metadata.
- The right-side detail panel should default to 36% width and remain independently scrollable from the findings list.
- Other legacy surfaces such as terminal-first flows, generic tabs, and multi-tool navigation must remain visually secondary or hidden from the first-run path.

---

## Component Inventory

- `Repo home`: centered intake card with folder picker, support status, detected build tool, and single primary CTA.
- `Unsupported repo state`: blocking empty-state panel with explicit reason and one recovery button.
- `Repo header bar`: persistent compact top bar with repo name and build tool badge.
- `Analysis progress strip`: inline progress bar, status label, and watch-only message.
- `Findings queue`: compact ranked list using card or item primitives with badge, title, rationale, and priority signal.
- `Finding detail panel`: right sheet/panel with rationale, principle-aware explanation, and affected files.
- `Affected files list`: compact filename list with monospace path treatment and no edit affordances.
- `Skeleton loading state`: 5 to 7 compact placeholder rows during live analysis.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact metadata spacing, badge gaps |
| md | 16px | Default card padding and form spacing |
| lg | 24px | Panel padding and section spacing |
| xl | 32px | Dashboard column gutters |
| 2xl | 48px | Entry-state and rejection-state breathing room |
| 3xl | 64px | Page-level centering space for intake states |

Exceptions: 40px persistent top bar height; 28px square compact icon buttons in the desktop shell; 36px minimum height for the primary `Start full analysis` button.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 | 1.5 |
| Label | 12px | 600 | 1.35 |
| Heading | 20px | 600 | 1.2 |
| Display | 28px | 600 | 1.15 |

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `var(--background)` / `oklch(1 0 0)` | App background, repo home canvas, dashboard canvas |
| Secondary (30%) | `var(--card)` with `var(--muted)` support | Cards, top bar, findings rows, detail panel, unsupported state panel |
| Accent (10%) | `var(--primary)` / `oklch(0.218 0.008 223.9)` | Primary CTA, active finding row, top-priority marker, progress emphasis |
| Destructive | `var(--destructive)` / `oklch(0.577 0.245 27.325)` | Unsupported-state warning iconography and destructive copy only |

Accent reserved for: `Start full analysis`, active finding selection state, top-priority emphasis, inline analysis progress, and focus-visible rings. Do not use accent for every badge, all navigation, or low-priority metadata.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | Start full analysis |
| Empty state heading | Repository ready for analysis |
| Empty state body | We detected a supported Maven or Gradle root. Start full analysis to build the ranked refactor findings queue. |
| Error state | This folder is not supported in Phase 1. Choose another folder whose selected root contains `pom.xml`, `build.gradle`, or `build.gradle.kts`. |
| Destructive confirmation | None: Phase 1 is watch-only and must not expose repository write actions. |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | `button`, `badge`, `card`, `empty`, `progress`, `scroll-area`, `sheet`, `skeleton`, `tooltip` | not required |
| `@ai-elements` | none in Phase 1 shell | `npx shadcn view @ai-elements/message` verified registry address on 2026-06-01; installed `src/components/ai-elements/*` scanned for `fetch`, `XMLHttpRequest`, `sendBeacon`, `process.env`, `eval`, `Function`, and external URL imports with no flags on 2026-06-01 |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
